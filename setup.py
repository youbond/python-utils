from setuptools import setup

setup(
    name='origin_common',
    version='0.0.1',
    description='Common util functions and mixins to make development of other projects easier.',
    url='https://git.originmarkets.com:48201/originmarkets/common',
    packages=['origin_common'],
    zip_safe=True,
    tests_require=['tox'],
)
